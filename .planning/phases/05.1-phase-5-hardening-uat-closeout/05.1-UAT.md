---
status: diagnosed
phase: 05.1-phase-5-hardening-uat-closeout
source: [05.1-VERIFICATION.md]
started: 2026-06-24T00:00:00Z
updated: 2026-06-24T21:10:00Z
---

## Current Test

[testing complete]

## Tests

### 1. HARDEN-01: datagen path completes CAP-01 capacity check without E201
expected: |
  Init a results directory, then invoke the datagen entrypoint that previously
  crashed with E201. The capacity check (CAP-01) completes — either by lazy-
  collecting `cluster_information` and computing a real required-bytes value,
  OR by gracefully degrading to a 0-byte no-op gate with a single
  "CAP-01 deferred: unable to determine system memory ..." INFO log on the
  operator's terminal. In either branch, the benchmark proceeds into actual
  data generation; no `AttributeError: 'TrainingBenchmark' object has no
  attribute 'cluster_information'` ever appears on stderr or in the log file.
command: |
  ./mlpstorage init BigCo /tmp/nerf-results && \
  ./mlpstorage closed training unet3d datagen file \
    -rd /tmp/nerf-results \
    -np 4 \
    -sn BigMachine \
    -dd /tmp/nerf-data
result: issue
reported: |
  `mlpstorage init BigCo /tmp/nerf-results` succeeded — wrote
  `/tmp/nerf-results/mlperf-results.yaml` with `orgname: BigCo`. But the
  subsequent `mlpstorage closed training unet3d datagen file -rd /tmp/nerf-results ...`
  exited immediately with:
    [E101] MLPSTORAGE_ORGNAME environment variable is required for closed|open runs
    Suggestion: export MLPSTORAGE_ORGNAME=<your_org>  # future: mlpstorage init <orgname> <results_dir>
  The capacity check never ran — HARDEN-01 could not be exercised because the
  E101 env-var gate fires before the cluster_information/CAP-01 path is reached.
  The init flow's on-disk mlperf-results.yaml is not being consumed by the
  closed/open env-var validator.
severity: blocker
why_human: |
  Verifies the full fix-train (754763a + 29f1062 + 5.1's RED/GREEN pair
  d13b418 + e5b7b8b — HARDEN-01) on the actual CLI invocation path, not
  just the unit-level mock. The unit tests in
  tests/unit/test_capacity_gate.py::TestTrainingBenchmarkRequiredBytes
  exercise the in-process Python contract; this UAT exercises argparse +
  registry dispatch + Benchmark._pre_execution_gate wiring + real
  accumulate_host_info path, which only run end-to-end from the CLI.
resolved_by: |
  754763a, 29f1062, d13b418, e5b7b8b (HARDEN-01 fix-train), 795a088 (RED),
  3dbc2d3 (GREEN — HARDEN-03 closes the secondary init↔env-validator
  blocker surfaced by this test)
harden_03_addendum: |
  The above HARDEN-01 closure assumed the env-var gate was already passable.
  Phase 5.1 UAT Test 1 (above) ALSO surfaced a secondary blocker: even after
  `mlpstorage init BigCo /tmp/nerf-results` succeeded, the subsequent
  `mlpstorage closed training unet3d datagen file -rd ...` still failed E101
  because `capture_or_verify_code_image` at code_image.py:552 was reading
  MLPSTORAGE_ORGNAME from env only — never consulting args.orgname (which
  main._main_impl's LAY-03 gate had populated from the sentinel by then).
  HARDEN-03 closes that gap: 795a088 (RED) + 3dbc2d3 (GREEN).
  Test 1's `command:` block is now end-to-end-green: init writes sentinel →
  closed/datagen reads args.orgname from sentinel → CAP-01 capacity check
  runs without crashing (HARDEN-01).

### 2. HARDEN-02: CAP-02 shared-FS probe transmits rank-0 result via stdout markers
expected: |
  When the launcher invokes the probe via `mpirun --tag-output --host 127.0.0.1:1,127.0.0.1:1 ...`
  with the new 2-positional argv (data_dir, run_uuid), result.stdout MUST contain the
  `__CAP02_RESULT_BEGIN__` / `__CAP02_RESULT_END__` markers and a parseable JSON payload between them.
  The launcher's regex-extract path MUST succeed and json.loads MUST yield `status: "ok"`.
  The launcher MUST NOT raise `"mpi4py not installed on all hosts"` when the probe semantically succeeded.
command: |
  pytest tests/integration/test_shared_fs_probe_real_mpi.py::TestSharedFsProbeRealMpi::test_cr02_rank0_result_arrives_via_stdout_markers_not_file -v
  # Real-multi-host equivalent (carry-forward — deferred to 05-UAT.md Test 4 hardware cycle per D-58):
  # ./mlpstorage closed training unet3d datagen file -rd <init'd> -np 4 -sn <sys> -dd <shared-mount> --hosts client1,client2 --num-processes 2
  # (launched from a laptop NOT in --hosts; rank 0 lands on client1; CAP-02 must succeed silently or report the real cause)
result: issue
reported: |
  After installing mpi4py 4.1.2 into .venv and running:
    .venv/bin/python -m pytest -v tests/integration/test_shared_fs_probe_real_mpi.py -k cr02_rank0_result_arrives_via_stdout_markers_not_file
  the test ran (no longer SKIPPED) and FAILED at line 281 with:
    json.decoder.JSONDecodeError: Expecting value: line 1 column 1 (char 0)
  This means the BEGIN/END marker assertions (lines 254-258) PASSED, but the
  payload extracted by the regex at lines 273-280 was empty after the
  `[host:rank]` tag-prefix strip — i.e. no JSON body landed between
  __CAP02_RESULT_BEGIN__ and __CAP02_RESULT_END__ under the
  --tag-output --host 127.0.0.1:1,127.0.0.1:1 scenario the test simulates.
  This is exactly the REVIEW-CR-02 shape HARDEN-02 was meant to lock; the
  stdout-marker transport is emitting empty payloads in the simulated
  remote-rank-0 case.
severity: blocker
why_human: |
  The localhost test (`pytest ...test_cr02_rank0_result_arrives_via_stdout_markers_not_file`)
  proves the stdout-marker transport works end-to-end with real mpirun on the dev box.
  The real-multi-host shape is deferred to the hardware-verification cycle (carry-forward of
  05-UAT.md Test 4 per D-58) — phase 5.1 sign-off does NOT require the multi-host repro.
artifacts:
  - mlpstorage_py/cluster_collector.py (probe heredoc + run_shared_fs_probe launcher — stdout transport landed)
  - tests/unit/test_cluster_collector.py::TestSharedFsProbeNonRank0Silence (structural defense for non-rank-0 silence)
  - tests/integration/test_shared_fs_probe_real_mpi.py::test_cr02_rank0_result_arrives_via_stdout_markers_not_file (localhost real-mpirun proof)
resolved_by: |
  f55fd5d (RED), 086b2a9 (GREEN)
  (closes REVIEW-CR-02 in .planning/phases/05-logical-diff-lifecycle-capacity-gate/05-REVIEW.md;
  the launch-host-local output_file is gone; rank-0 result arrives via stdout regardless of
  which host rank 0 lands on)

## Summary

total: 2
passed: 0
issues: 2
pending: 0
skipped: 0
blocked: 0

## Gaps

- truth: "After `mlpstorage init <org> <results-dir>`, subsequent `mlpstorage closed|open ...` invocations targeting that results dir satisfy the orgname requirement WITHOUT needing MLPSTORAGE_ORGNAME exported, by reading mlperf-results.yaml from -rd"
  status: failed
  reason: "User reported: init wrote mlperf-results.yaml with orgname:BigCo to /tmp/nerf-results, but the next closed-training invocation with `-rd /tmp/nerf-results` still failed with E101 MLPSTORAGE_ORGNAME required. The init artifact on disk is not being consumed by the closed/open env-var validator. As a side effect, HARDEN-01's CAP-01/cluster_information code path could not be exercised end-to-end via CLI — the run halted before reaching it."
  severity: blocker
  test: 1
  root_cause: "mlpstorage_py/submission_checker/tools/code_image.py:552-562 (capture_or_verify_code_image) reads MLPSTORAGE_ORGNAME from env only and raises E101 when absent. It never consults args.orgname, which main.py's LAY-03 gate at main.py:356-389 has already populated from the on-disk sentinel via resolve_orgname(results_dir_value). The literal 'future: mlpstorage init <orgname> <results_dir>' suggestion text at code_image.py:559 is a fossil from before Slice 4 (sentinel-resolution gate) landed in main.py — main.py was updated to consume the sentinel, but the helper was not. Symmetric SYSTEMNAME block at code_image.py:587-597 has the same shape for OPEN mode."
  artifacts:
    - path: "mlpstorage_py/submission_checker/tools/code_image.py:552-562"
      issue: "Reads MLPSTORAGE_ORGNAME from env without falling back to args.orgname"
    - path: "mlpstorage_py/submission_checker/tools/code_image.py:559"
      issue: "Stale '# future: mlpstorage init <orgname> <results_dir>' suggestion contradicts shipped LAY-03 reality"
    - path: "mlpstorage_py/submission_checker/tools/code_image.py:587-597"
      issue: "Symmetric SYSTEMNAME block for OPEN mode has same env-only shape"
    - path: "mlpstorage_py/main.py:224"
      issue: "Passes os.environ to helper after args.orgname is already populated by LAY-03 gate"
    - path: "mlpstorage_py/tests/test_capture_or_verify_code_image.py:133"
      issue: "Under-constrained — asserts E101 when no sentinel present; no sibling test that asserts SUCCESS when args.orgname populated and env empty"
    - path: "tests/integration/test_canonical_layout_end_to_end.py:134"
      issue: "test_init_then_run_closed only invokes capture_code_image() not the full CLI dispatch — does not exercise capture_or_verify_code_image"
  missing:
    - "Prefer getattr(args, 'orgname', None) before env.get(MLPSTORAGE_ORGNAME_ENVVAR) in capture_or_verify_code_image"
    - "Same precedence change for SYSTEMNAME (symmetric block, OPEN mode)"
    - "Update suggestion text at code_image.py:559 — drop '# future:' annotation; mention `mlpstorage init` as preferred path"
    - "RED-first integration test driving full CLI path: `mlpstorage init BigCo <rd> && mlpstorage closed training unet3d datagen file -rd <rd> ...` with MLPSTORAGE_ORGNAME unset; asserts no E101 raised"
  debug_session: ".planning/debug/init-orgname-not-consumed-by-env-validator.md"

- truth: "Under `mpirun --tag-output --host 127.0.0.1:1,127.0.0.1:1 -n 2`, the SHARED_FS_PROBE_SCRIPT heredoc emits the rank-0 result JSON between __CAP02_RESULT_BEGIN__ / __CAP02_RESULT_END__ markers on stdout AND the launcher's tag-strip regex extracts a parseable JSON payload from the captured stdout"
  status: failed
  reason: "User reported: BEGIN/END marker assertions passed (lines 254-258) but json.loads at line 281 raised JSONDecodeError 'Expecting value: line 1 column 1 (char 0)'. The captured group between the markers is empty after the tag-strip — no JSON body is landing between the markers in the --tag-output + multi-host scenario per the original framing. Diagnosis revealed the payload IS in stdout; the tag-strip regex is what's broken."
  severity: blocker
  test: 2
  root_cause: "Probe-side emission is correct — rank 0 (only, per the `if rank == 0:` guard at cluster_collector.py:2829) prints BEGIN, compact one-line JSON, END, all with flush=True. The bug is in the consumer-side tag-strip regex `re.sub(r'^\\[[^\\]]+\\]\\s*', '', payload)`, which was written assuming OpenMPI's --tag-output prefix is `[host:rank] ` (bracketed identifier + whitespace). The actual OpenMPI 4.x format (verified on 4.1.6, dev box) is `[rank,jobid]<channel>:` where the channel marker `<stdout>:` / `<stderr>:` / `<stddiag>:` is glued directly to the bracketed identifier with NO whitespace. The regex strips `[1,0]` but leaves `<stdout>:` glued in front of the JSON; `json.loads('<stdout>:{...}')` raises JSONDecodeError because `<` is not a valid JSON token start. Reproduced literal stdout: `'[1,0]<stdout>:__CAP02_RESULT_BEGIN__\\n[1,0]<stdout>:{\"status\":\"ok\",...}\\n[1,0]<stdout>:__CAP02_RESULT_END__\\n'`. The bug exists in BOTH the production launcher and all four integration tests — a real multi-host production CAP-02 would also fail by raising FileSystemError. The HARDEN-02 GREEN at 086b2a9 shipped this bug."
  artifacts:
    - path: "mlpstorage_py/cluster_collector.py:3542"
      issue: "Production launcher tag-strip regex `re.sub(r'^\\[[^\\]]+\\]\\s*', '', _payload_raw)` leaves `<stdout>:` residual on OpenMPI 4.x output"
    - path: "mlpstorage_py/cluster_collector.py:3512-3514"
      issue: "Surrounding comment claims --tag-output prefix is `[hostname:rank]`; actual format is `[rank,jobid]<channel>:` — comment is wrong"
    - path: "tests/integration/test_shared_fs_probe_real_mpi.py:138"
      issue: "test_two_local_ranks_same_tmpfs_succeeds_silently uses same broken regex — also fails"
    - path: "tests/integration/test_shared_fs_probe_real_mpi.py:206"
      issue: "test_d49_quiesce_observable_via_wall_clock — same broken regex"
    - path: "tests/integration/test_shared_fs_probe_real_mpi.py:280"
      issue: "test_cr02_rank0_result_arrives_via_stdout_markers_not_file — the UAT-reported failure"
    - path: "tests/integration/test_shared_fs_probe_real_mpi.py:323"
      issue: "test_two_local_ranks_outputs_carry_correct_uuid — same broken regex"
  missing:
    - "Replace tag-strip regex with `r'^\\[[^\\]]+\\](?:<[a-z]+>:?)?\\s*'` to consume optional `<stdout>` / `<stderr>` / `<stddiag>` channel marker (with or without trailing `:`)"
    - "Apply same regex fix to all four integration test sites; ideally factor into a shared helper to prevent future drift"
    - "Update misleading `[hostname:rank]` comment at cluster_collector.py:3512-3514 to reflect actual `[rank,jobid]<channel>:` OpenMPI format"
    - "Add unit-layer regression test feeding captured `--tag-output` byte-string fixture (with literal `<stdout>:` channel marker) through tag-strip + json.loads — locks the contract without requiring mpirun"
  debug_session: ".planning/debug/cap02-stdout-empty-payload-tag-output-multihost.md"
